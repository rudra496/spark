# Internationalization

Spark ships with a minimal i18n helper in `spark.i18n`.

## Supported Locales

- `en` (English)
- `es` (Spanish)

## Usage

```python
from spark.i18n import translate

print(translate("validation_ok", locale="es"))
```

Unknown locales fall back to English.
